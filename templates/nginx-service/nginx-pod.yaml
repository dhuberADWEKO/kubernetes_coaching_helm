apiVersion: v1
kind: Pod
metadata:
    name: nginx-pod
    labels:
        app: nginx-app # Verwende ein spezifisches Label für NGINX
spec:
    containers:
        - name: nginx
          image: nginx:latest
          ports:
              - containerPort: 80