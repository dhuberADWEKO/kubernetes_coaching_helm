apiVersion: v1
kind: Service
metadata:
    name: {{ include "my-helm-chart.fullname" . }}-nodeport
    labels:
        app: {{ include "my-helm-chart.name" . }}
spec:
    type: NodePort
    selector:
        app: {{ include "my-helm-chart.name" . }}
    ports:
        - port: 80
          targetPort: 80
          protocol: TCP
          name: http
          nodePort: 30036
